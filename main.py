from utilities.dice import Dice
from character.character import Character
from utilities.string_utilities import write_positive

# create a dice object with your desired
# amount of sides
# dice = Dice()
# attrib = dice.roll(3)
# print(f"The output of the die roll is: {attrib}")

# Ternary in most languages
# var i = "fish" : "true fish" ? "false fish";
